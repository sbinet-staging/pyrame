============================
The "Add clock" uncap plugin
============================

Any time this plugin gets a data packet (delimited by a cariage return), it concatenate the clock and this packet and output it.
This is useful to tag the data with some clock extracted from another signal.
