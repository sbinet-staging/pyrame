================================
The "extract clock" uncap plugin
================================

This plugin extract every pattern delimited by a cariage return and insert it as a clock in the acquisition chain.
